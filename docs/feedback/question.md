# 使用反馈
1. Markdown编辑器问题反馈

* 深色模式下  > 引用类型的块，背景看不清是纯白， 建议使用左边一个长的线代表引用，这种Markdown解析方式比较常见 Obsidian 也是用的这个方式
*  `` 这种代码块的文本后的背景没有 Padding，紧紧贴着字，很难看，没有圆角也很难看
* ``` 代码块有同样的问的，也没有代码高亮能力
* 编辑器滚动条 没有在最右边，居然跟着文章右边界
* 进入编辑态，再退出后，左边的目录消失了
* 在编辑器里很多地方空格键没反应，但是又占了位，比如 # xxx 的最后
