name: markdown_editor
description: Markdown editing widgets for MVision.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  design_system:
    path: ../design_system
  markdown_engine:
    path: ../markdown_engine

dev_dependencies:
  flutter_test:
    sdk: flutter
